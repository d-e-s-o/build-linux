# Configuration fragment providing a kernel module for the `modules`
# input to install. `dummy` is about as small and dependency free as
# loadable modules get.
CONFIG_MODULES=y
CONFIG_MODULE_UNLOAD=y
CONFIG_DUMMY=m
